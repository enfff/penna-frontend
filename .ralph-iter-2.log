Now I'll implement T7. First, checking the exact `adw::Toast::new` signature available in the vendored bindings before writing code:
Signature confirmed. Now adding the pure counter to `conflict.rs`:
Now appending the save-guard test module at the end of `conflict.rs`:
Now wiring the guard into `save_current_entry` in `window.rs`:
Running the gate command plus the test suite:
Gate is clean, but let me confirm the new tests are actually running:
The append edit left stray braces. Inspecting the seam to fix:
The seam is mangled — restoring correct brace structure:
All 6 new tests pass. Verifying `conflict.rs` itself is rustfmt-clean (application.rs diff is pre-existing) and running the full gate:
Gate passes, 65/65 tests green (rustfmt noise is pre-existing repo-wide; gate is clippy-only). Reviewing the final diff for sanity:
Diff is clean and scoped. Marking T7 done in `PLAN.md` and appending the progress line:
**T7 complete — Save guard**

- Added `unresolved_conflict_count()` to `src/conflict.rs:326` — counts well-formed conflict blocks (one `Current` segment per block, so back-to-back blocks and empty sides count correctly); 6 new unit tests covering clean prose, malformed lookalikes, single/multiple/back-to-back blocks, empty sides.
- Guarded `save_current_entry` in `src/window.rs:1860`: Ctrl+S with unresolved conflicts shows a High-priority `"N unresolved conflicts"` toast via the existing `toast_overlay` and refuses to save; a resolved note falls through to `entry_save` untouched.
- Gate (`cargo clippy --all-targets -- -D warnings`) passes; 65/65 tests green.
- T7 marked `[x]` in PLAN.md; progress line appended. Next iteration picks up T8 (sync surface).
