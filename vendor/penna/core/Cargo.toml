[package]
name = "penna-core"
version = "0.1.0"
edition = "2024"
description = "Penna core domain and application logic"
license = "UNLICENSED"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
