First iteration (progress.txt is empty). Task: **T16 — Settings layer**. Let me survey the settings usage.
I have the full picture. Now checking remaining `gio` usages so imports stay tight:
gio 0.22 setters return `Result<(), glib::error::BoolError>`. Now creating the settings module:
Now wiring it up — main.rs module declaration, then window.rs call sites:
Now application.rs:
Running the gate:
Missing trait import:
Gate green. Verifying no stragglers and recording:
**T16 complete.**

- **What moved**: all six `SETTINGS_*` key constants plus a thin `gio::Settings` wrapper (`get_str`/`set_str`/`get_bool`/`set_bool`) into new `src/settings.rs`; the duplicate `SETTINGS_SCHEMA_ID` constants in window.rs and application.rs were deleted (now defined once in settings.rs). All 18 call sites across window.rs and application.rs go through `crate::settings`. No direct `gio::Settings` usage remains outside settings.rs.
- **Line counts**: settings.rs 38 (new), window.rs 4102 (was 4114), application.rs 562 (was 575).
- **Gate**: clippy `-D warnings` clean, 82 tests pass.
- Behavior freeze respected — pure mechanical move, no msgid or .ui changes, no git operations.
