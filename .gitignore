# Rust build output
/target/

# Project-local cargo cache (pre-seeded engine git checkout, registry)
.cargo/

# Meson / GNOME Builder build directories
/_build/
/build/

# Flatpak Builder cache/output
/.flatpak-builder/

# Editor/OS temporary files
*~
*.swp
*.swo
.DS_Store

# Local environment files
.env
.env.*

# Copy of our penna engine, temporary until we can get it published to crates.io
vendor/

*.txt

# Ralph loop artifacts
.ralph-iter-*.log
progress.txt
# Autonomous loop tooling (kept out of the repository)
ralph.sh
PROMPT.md
PLAN.md
